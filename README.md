# CBR Currency Course Viewer Frontend 

Based on React.

### Installation

1. `git clone`
2. `yarn`
3. Create `.env` based on `.env.example`

### How to run

1. `yarn start` - for local serving
2. `yarn build`, then serve static files with any webserver in `/build` directory 
